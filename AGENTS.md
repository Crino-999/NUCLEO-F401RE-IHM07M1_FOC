# AGENTS.md — 仓库级协作约定（公开仓库）

> 本文件是 Codex 在本仓库工作的最高优先级行为指令。每次会话开始必须先完成第 2 节必读，再开始任何工作。
> 本文件条目与历史对话、旧文档冲突时，以本文件与用户最新指示为准。

## 1. 项目定位

NUCLEO-F401RE + X-NUCLEO-IHM07M1 平台上的 FOC 电机控制学习与工程仓库。
价值排序：**收获知识 > 文档完善 > 代码可理解 > 项目跑通**。
本仓库会推送到 GitHub **公开仓库**，所有提交内容默认会被陌生人长期查看。

## 2. 开工必读（按顺序，勿跳过）

1. `docs/01-计划/学习总计划.md` —— 路线图、里程碑、git 与文档约定
2. `docs/01-计划/项目状态与Codex协作交接.md` —— 当前进度、关键文件、硬性约束
3. `docs/01-计划/决策记录.md` —— 只以「状态 = 有效」的行作为设计基线；已推翻 / 待定行仅作背景
4. 与本次任务相关的笔记、代码、实验记录（按需）

## 3. 当前意图仲裁规则（防漂移，必守）

优先级从高到低：

1. 用户本次会话的明确指示（含推翻 / 变更）
2. `项目状态与Codex协作交接.md` 中「当前有效决定」与最新变更记录
3. `决策记录.md` 中「有效」行（编号最新者优先）
4. 笔记 / 代码 / 旧文档（仅参考，不构成设计基线）

冲突处理：

- 发现 1–3 与旧文档 / 代码冲突时，以 1–3 为准，并向用户列出冲突清单，不得默默沿用旧设计。
- 用户推翻某项设计后，必须同步三处：交接文档 → `决策记录.md`（新增一行）→ 原文档修订记录。缺一即视为未完成。

## 4. 硬性约束

- **不代写 FOC 核心算法代码**（坐标变换、SVPWM、PID、观测器等）。AI 角色：原理讲解、代码审阅、调试排错、文档梳理。
- **不虚构**学习进度、实验数据、验证结论；实验结果一律以用户提供为准。
- 修改任何笔记后，按笔记自身约定追加修订记录（电机原理笔记须在第七章追加）。
- 任何设计变更必须在 `docs/01-计划/决策记录.md` 登记。
- 不直接执行 `git push`；本地 `commit` 必须通过第 5 节隐私审查且用户确认后进行。

## 5. 提交前隐私审查（最高优先级门槛）

本仓库面向公开，**任何 commit 之前**必须通过以下三层检查，不依赖对话记忆。

### 5.1 L1 关键词 / 文件扫描（本地命令）

按 `学习总计划.md` 8.6 节 L1 执行，针对 **staged 变更**：

- 文件名、变更内容不得命中 `.git_sensitive_words.txt`（该文件永不提交）
- 不得出现本机绝对路径、Windows 用户名、真实邮箱 / 电话
- 提交者必须是 GitHub 隐私邮箱（noreply 格式）

### 5.2 L2 内容语义审查（重点）

关键词扫描只是底线；真正要判断的是**实际描述的内容是否适合公开**。
按 `学习总计划.md` 8.6 节 L2 清单逐项检查，典型红线：真实身份 / 雇主信息、职业隐私（职业规划、目标公司与岗位要求原文、收入信息等内部细节）、与 AI 的对话过程、未审阅草稿、凭据。

三个自检问题：

1. 陌生人能据此推断出真实身份、雇主或位置吗？
2. 招聘方看到会看到我不想公开的职业信息吗？
3. 放到公开仓库里，现在和将来都不后悔吗？

### 5.3 执行方式（L2 默认由主代理执行；本地 Worker 仅做初筛）

L2 属于判断 / 审核类工作，按本机全局约定留在云端由主代理完成：

- commit 前，主代理按 8.6 L2 清单逐项审查 staged 变更，输出「可提交 / 待确认 / 不可提交 + 文件:行号 + 理由」，交用户终审。
- 本地 Ollama MCP Worker（vision / doc-worker）仅用于机械初筛（截图文字识别、文档要点提取），可用时调用，敏感内容一律传文件路径，不粘贴正文；不可用时跳过，不影响审查。
- 如需独立复核，可另派 Codex 子代理（`spawn_agent`，`fork_turns="none"`，只传文件路径清单，不传历史对话）。**注意：Codex 子代理走云端模型，不是本地 Worker**。
- 用户终审是唯一放行依据；AI 生成的新文档 / 代码与用户手写内容同等对待，一律过审。

Codex 子代理任务模板（可选独立复核，云端）：

> 只读审查以下 staged 文件路径清单，不得修改任何文件：<路径列表>。
> 按 `docs/01-计划/学习总计划.md` 8.6 节 L2 清单，逐项判断每个文件的实际描述内容是否适合提交到公开 GitHub 仓库。
> 输出「可提交 / 待确认 / 不可提交 + 文件:行号 + 理由」表格；只输出结论与位置，不要输出原文内容。

### 5.4 与推送检查的关系

- commit 前：L1 + L2（staged 变更）
- push 前：8.6 全量检查（HEAD 全部历史）+ 推送后远程克隆复核

## 6. 文档与 Git 约定（详见学习总计划）

- 提交前缀：`docs:` / `feat:` / `fix:` / `refactor:` / `chore:`
- 变更类提交同时更新对应文档与 `决策记录.md`
- 工作区保持干净；`.vscode/`、构建产物、`.git_sensitive_words.txt` 禁止提交
- 分支：`main` 为主，实验性修改用临时分支，验证后合回或删除

## 7. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-16 | 初稿：建立仓库级协作约定，含防漂移仲裁规则与提交前隐私审查机制 |
| 2026-08-16 | L2 执行方式修正：默认由主代理执行，本地 Ollama Worker 仅做机械初筛，Codex 子代理为可选云端复核 |
